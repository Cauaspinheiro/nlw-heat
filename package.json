{
  "scripts": {
    "start:backend": "cd backend && yarn start",
    "start:mobile": "cd mobile && expo start",
    "start:web": "cd web && yarn dev",
    "start:all": "npm-run-all -p start:backend start:mobile start:web"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
